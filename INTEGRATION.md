# 前后端联调契约

> **这篇文档是「前端 ↔ Java」那一层的接口定义。**
> 它存在的理由：`DEV_DOC §3.4` 把 **Java → Python** 写得很细，
> 而 **前端 → Java** 只在 §3.1 的图里有一行 `│ /api/v1/** (JWT Bearer) │` ——
> **一个 endpoint 都没定义。**
>
> 所以本文不是"记录现状"，是**产出那层缺失的定义**。

---

## 0. 这份文档是什么 / 不是什么

⚠️ **本文最大的风险是变成 `DEV_DOC` 的镜像。**

`DEV_DOC §3.4` 那三个 Python 接口已经写得很细了。如果在这儿**再抄一遍**，
两份必然漂移 —— 而这正是本项目反模式里的「镜像双后端必然漂移」（附录 A「明确不抄」），
只是发生在文档层。

所以划清界限：

| ✅ 本文写 | ❌ 本文不写（只给指针）|
|---|---|
| **前端 → Java** 的接口（现在没有）| Java → Python 的接口（§3.4）|
| 每个前端交互对应哪个接口 | `outline` 的字段定义（§4.2）|
| **前端必须改哪里** | 布局算法（ADR-6）|
| SSE 事件的**具体形状**（§5.4 只定了约定）| 状态机本身（§5.2）|
| 联调顺序 + 验证清单 | 表结构（§4.1）|

**冲突时以 `DEV_DOC` 为准**（同 `ARCHITECTURE.md` 的从属声明）。

### 记号约定

| 写法 | 指向 |
|---|---|
| **`§4.2`**（带 §）| **一律指 `DEV_DOC` 的小节** |
| **「本文 3.2 节」** | 本文自己的小节 |

⚠️ 这个区分必须守住：**`§` 在本项目里的全部意义就是"指向权威文档"**。
本文自己的小节如果也用 `§`，读者就分不出"这条规矩出处在哪"和
"这里再展开了一点" —— 而前者才是他要的东西。

---

## 1. 三条边界，谁定义了什么

```
┌─ React 前端 ────────────────────────────────┐
│  渲染 · 对话 · 结构性编辑 · SSE 订阅          │
└──────────────┬──────────────────────────────┘
               │  ❶  前端 → Java   （本文产出）
               │      /api/v1/**  JWT Bearer
┌──────────────▼─ Java Host ──────────────────┐
│  鉴权 · 持久化 · revision 锁 · Policy        │
│  审批流 · 调度 · 审计 · SSE 推送              │
└──────────────┬──────────────────────────────┘
               │  ❷  Java → Python  （§3.4 已定义）
               │      内部 HTTP，无状态、幂等
┌──────────────▼─ Python 引擎 ────────────────┐
│  规划 · 单任务执行 · 改稿 · RAG · MCP        │
│  ⚑ 不连库、不存会话、不知道 revision          │
└─────────────────────────────────────────────┘
```

| 边界 | 状态 |
|---|---|
| ❶ 前端 → Java | ❌ **未定义** —— 本文产出 |
| ❷ Java → Python | ✅ §3.4（`/v1/plan`、`/v1/execute_task`、`/v1/revise`）|
| ❸ **前端内部的算法层** | ✅ 已存在，且**永远不会变成接口** |

**❸ 值得单独说**：`lib/layout.ts`（布局）、`lib/outline.ts`（挂载 + 校验）是**纯前端**的。
ADR-6 明确「布局只在前端，后端不存坐标」。
**接后端时这两个文件一行都不用改** —— 本文第 6 节会回过头验证这句话。

---

## 2. 前端现在有哪 9 处是假的

这张表是本文的核心 —— **每一行都是一个待接的接口。**

| # | 位置 | 现在（假）| 真实应该是 |
|---|---|---|---|
| 1 | `App.submit()` | `pickDataset(goal)` **猜**数据集 | `POST /api/v1/maps`（真规划）|
| 2 | `App` 的 planning effect | `setTimeout(700ms)` 假延迟 | 上一条的响应 |
| 3 | `App` 的 executing effect | `advance()` 每 700ms 本地推进 | **Java 调度**，前端只收 SSE |
| 4 | `mocks/index.ts` 的 `japanExecution` | 手写的"各节点产出什么" | `POST /v1/execute_task` 的返回 |
| 5 | `handleNodeAction()` | 本地改 `outline` | 三个操作接口（见本文 3.2 节）|
| 6 | `handleMove()` | `moveNode()` 本地直接生效 | `PATCH .../outline` + `revision` |
| 7 | `handleDelete()` | `deleteNode()` 本地直接生效 | 同上 |
| 8 | `lib/summary.ts` | 前端自己算 | §5.2 说 Java 返回 `"summary"` |
| 9 | `mocks/datasets` + 数据集切换器 | demo 专用的数据集选择 | **删掉**（那是演示脚手架）|

**⚠️ 第 6、7 行是风险最大的**：结构性编辑现在**改完立刻生效**，
而真实系统要有 `revision` 乐观锁、409、按节点串行保存队列（§5.3 铁律 #2 / #4）。
**这是 demo 与真实系统差距最大的一处** —— 单机演示下感觉不到差别。

---

## 3. 前端 → Java 接口

### 3.0 为什么不是一个 `action` 接口 —— 两道网

先摆一个看着很香的方案：

```
POST /api/v1/maps/{mapId}/nodes/{nodeId}/actions
     { "action": "approve" | "reject" | "complete" | "handle" | "discard" }
```

一个接口覆盖全部五种操作，前端一处调用、加动作不用改路径。

**它有个致命的坏处**，而且和这个项目的一贯立场直接冲突。

#### 坏在哪：把三个不同的领域操作压进了一个字段

服务端会变成这样：

```java
switch (action) {                  // ⚠️ action 是个【字符串】
  case "approve"  -> ...
  case "reject"   -> ...
  case "complete" -> node.status = "done";
  case "handle"   -> node.assignee = "user"; node.status = "todo";
  case "discard"  -> node.assignee = "user"; node.status = "skipped"; cascade(...);
}
```

**问题在第 5 行往下。** 以后有人想加个「取消任务」，顺手写一行：

```java
  case "cancel"   -> node.status = "skipped";     // ← 绕过了状态机
```

**能写，而且没有任何东西拦着。**

而 `todo → skipped` **是合法的转移**（级联跳过就是它，§5.2）——
所以哪怕配一个状态机校验函数，它也会**放行**。这个错会一路滑到线上。

#### 这和 §4.2 是同一条原则

> **把 `assignee` / `status` / `approval` 拆成三个正交的轴，不要压进一个字段。**

理由一模一样：**压进一个字段之后，每一处读它的代码都被迫理解全部语义**
（§4.2 原话）。一个 `action: string` 接口，就是把这个错误搬到了接口层。

#### 两道网，各管一头

| | 管什么 | 管不住什么 |
|---|---|---|
| **接口划分** | 你能**表达**什么 | 表达出来之后，落地时是否合法 |
| **状态机校验函数** | 表达了之后，这个转移**能不能落地**（`done → running` 拦住）| 你的**理由**对不对 |

**两道网的分工，用一个例子说清楚：**

| 动作 | 转移 | 状态机怎么看 | 该不该允许 |
|---|---|---|---|
| 用户点「不处理」，放弃一个失败的任务 | `failed → user+skipped` | ✅ 合法 | ✅ 该 |
| 依赖上游被放弃，**级联跳过** | `todo → skipped` | ✅ 合法 | ✅ 该 |
| 「取消一个普通任务」 | `todo → skipped` | ✅ **也合法** | ❌ **不该** |

前两条和第三条**在状态机眼里长得一模一样**。状态机说不出区别 ——
它只看到"从 A 到 B 是允许的"。

> **区分它们靠的是"从哪个接口进来的"。**
> `POST .../failure-resolutions` **只能产出 `handle` / `discard`**，
> 它压根没有"取消一个普通任务"这个说法。想加，就得**新增一个接口** ——
> 而"新增接口"这件事本身会逼人把语义想清楚。

**所以：接口划分是第一道网（让非法状态无法表达），状态机校验是第二道。**
校验函数是最后一道网，**不是唯一一道**。

> ⚠️ 反过来说也成立：光有三个接口、没有状态机校验，
> 服务端代码里还是可能有人直接 `node.status = ...` 赋值。
> **两道都要。**

#### 顺带：三个接口正好对应界面上那三组待办

```
对话区「需要你决定」  →  .../failure-resolutions
对话区「等你点头」    →  .../approvals
对话区「需要你做」    →  .../completions
对话区「卡住了」      →  .../completions   （用户在系统外办完，回来标记）
```

**不是巧合** —— 那几组本来就是不同的人类决定（要判断 / 要点头 / 要动手）。

### 3.1 接口清单

| # | 方法 + 路径 | 用途 | 对应前端 |
|---|---|---|---|
| 1 | `POST /api/v1/maps` | 提交目标 → 规划出图 | `App.submit()` |
| 2 | `GET /api/v1/maps/{mapId}` | 取一张图 | 首次加载 / 重连 |
| 3 | `POST /api/v1/maps/{mapId}/start` | 用户确认，开始执行 | `App.confirm()` |
| 4 | `POST /api/v1/maps/{mapId}/approvals` | 批准 / 否决 | 「等你点头」|
| 5 | `POST /api/v1/maps/{mapId}/completions` | 人工标记完成 | 「需要你做」「卡住了」|
| 6 | `POST /api/v1/maps/{mapId}/failure-resolutions` | 失败处置 | 「需要你决定」|
| 7 | `PATCH /api/v1/maps/{mapId}/outline` | 结构性编辑（移动 / 删除）| 拖拽 + 删除 |
| 8 | `POST /api/v1/maps/{mapId}/proposals` | 对话改稿 → 提案 | 输入框（多轮调整）|
| 9 | `POST /api/v1/maps/{mapId}/proposals/{pid}/apply` | 应用提案 | 提案确认按钮 |
| 10 | `GET /api/v1/maps/{mapId}/events` | 订阅事件（SSE）| 执行进度 |

### 3.2 逐个说明

#### ⚠️ 先说一个批量问题 —— 它决定了 4/5/6 的形状

前端有**批量操作**：「全部批准（3）」「全部标记完成（2）」
（`lib/simulation.ts` 的 `approveAll()` / `markUserTasksDone()`）。

如果按最直觉的做法 —— 前端循环发 N 个单节点请求 —— 会撞上这个：

> `revision` 是**整图一个**（§4.1 的 `maps.revision`），
> 每次写入 +1，不匹配就 409（§5.3 铁律 #2）。
>
> 所以 N 个请求**并行**发、都带 `revision=7`：
> 第一个成功（→8），**其余 N-1 个全部 409**。

客户端串行发可以绕开（每次用上一个响应里的 revision），
但那样点一次「全部批准」就是 5 个来回。

**所以 4/5/6 收 `node_ids: string[]`** —— 一次请求、一次 revision 递增、一次原子应用。

> **单节点操作就是只有一个元素的数组。**
> 这样 body 形状**恒定**，不会一会儿是对象一会儿是数组。

---

#### 1 · `POST /api/v1/maps` 建图

```jsonc
// 请求
{ "goal": "帮我规划一次日本关西七日游" }

// 响应 201
{
  "mapId": "map_9f2c",
  "revision": 1,
  "status": "ready",          // draft | planning | executing | ready | failed
  "outline": [ /* §4.2 的扁平数组 */ ],
  "issues": [ /* §7.1 的 StructureIssue */ ],
  "summary": { /* §5.2 的"部分成功的呈现" */ }
}
```

⚠️ **`assignee` 在这里是 Agent 的【提议】**（§3.4 原文）——
真正的归属由 **Java 的 Policy 引擎**裁决（FR-2）。
所以响应里的 `assignee` 应当**已经是裁决后的值**，前端不参与裁决。

**前端要处理的**：`issues` 里有 `error` 时 → `hasBlockingIssue()` 为真 →
**「确认并开始执行」必须消失**（§7.1 的闸门，前端已实现，会继续生效）。

#### 2 · `GET /api/v1/maps/{mapId}` 取图

响应同上（去掉 201）。用于首次加载和 SSE 重连。

#### 3 · `POST /api/v1/maps/{mapId}/start` 开始执行

```jsonc
{ "revision": 1 }
→ 202 { "status": "executing", "revision": 2 }
```

⚠️ **服务端必须自己再判一次阻断**：`hasBlockingIssue(issues)` 为真就拒绝（409 或 422）。
前端的闸门是**提示**，不是保证 —— §7.1 说它是**唯一闸门**，那它就得在服务端也在。

#### 4 · `POST .../approvals` 批准 / 否决

```jsonc
{ "revision": 2, "decision": "approve", "node_ids": ["a3f9..."] }
→ 200 { "revision": 3, "outline": [...], "summary": {...} }
```

- `decision`：`approve` | `reject` —— **闭集合，只有两种**
- 拒绝 → 该节点 `assignee=user` + `status=todo` + `assignee_reason=user_rejected`（§5.2）
- ⚠️ 拒绝原因写进 `approvals` 表（§4.1），不是只改节点

#### 5 · `POST .../completions` 人工标记完成

```jsonc
{ "revision": 3, "node_ids": ["a3f9...", "7d2e..."] }
→ 200 { "revision": 4, ... }
```

**语义**：人宣告"这件事办完了"。

⚠️ 服务端必须检查：
- 这个节点归人（`assignee != agent`）—— **Agent 的活不能被人顶掉**，
  否则"Agent 有没有真做完"这个最关键的信息就被污染了
- 应同时置 `locked = true`（§5.3 铁律 #3：人工改过的 AI 不碰）
- **不要求 `evidence`** —— §7.3 的 `E_MISSING_EVIDENCE` 只针对 `assignee=agent`

#### 6 · `POST .../failure-resolutions` 失败处置

```jsonc
{ "revision": 4, "decision": "handle", "node_ids": ["a3f9..."] }
→ 200 { "revision": 5, ... }
```

- `decision`：`handle` | `discard` —— **闭集合，只有两种**
- `handle` → `assignee=user`, `status=todo`, `reason=agent_failed`（§5.2）
- `discard` → `assignee=user`, `status=skipped`, `reason=agent_failed`
  **+ 级联下游为 `skipped`**（§5.2）
- ⚠️ 只对 `status=failed` 且 `assignee=agent` 的节点有效

#### 7 · `PATCH /api/v1/maps/{mapId}/outline` 结构性编辑

这是**唯一一个需要前端算好再提交**的接口。§5.3 写得很明确：

> **客户端负责把受影响的两组 `order` 重编号好再提交，服务端只校验 `W_ORDER_INVALID`。**

```jsonc
// 请求
{
  "revision": 5,
  "upsert": [ { "id": "a3f9...", "parent_id": "7d2e...", "order": 2 } ],
  "remove": [ "b8c1..." ]
}
→ 200 { "revision": 6, "outline": [...], "issues": [...] }
```

**为什么是"改动子集"而不是整张图**：200 个节点每次都全传太重，
而客户端**本来就已经算出完整的新 `outline`** 了（`lib/outlineEdit.ts`），
取个 diff 是顺手的事。

**`upsert` 里只放【真正改过的字段】** —— 移动改 `parent_id`/`order`；
删除时被清理过依赖的节点改 `depends_on`。

⚠️ **服务端仍然要拿完整结果跑一遍 §7.2 ②③ 两组校验** ——
客户端算错了（或有别的写入并发）不能把坏数据落库。
校验不过 → **400 + `issues`**，前端把它显示在问题条上。

> **这里不存在"两端各实现一遍算法"的问题**：
> 客户端做的是**计算新结构**（重编号 + 护栏），
> 服务端做的是**校验结果**（`buildTree` 那套检查）。
> **是两个不同的算法**，和 §11 里「`lib/outline.ts` 与 Python `outline.py` 不是同一个算法」
> 是同一条注意事项。

#### 8 / 9 · 提案 → 应用（对话改稿）

```jsonc
// POST .../proposals
{ "instruction": "把第三点拆开", "history": [ /* ⚑ 上限 5 轮，客户端带 */ ] }
→ 200 { "proposalId": "p_1", "operations": [...], "issues": [...], "summary": "将新增 3 个节点，删除 1 个" }

// POST .../proposals/{pid}/apply
{ "revision": 6 }
→ 200 { "revision": 7, "outline": [...], "issues": [...] }
```

⚠️ **两段式是铁律**（§5.3 铁律 #1）：`proposals` **绝不写库**。
前端必须把 `summary` 那句话**摆给用户看**再让他点确认 ——
这也正是前端「删除确认块」已经在做的事。

⚠️ `history` 由**客户端**携带、上限 5 轮（§3.2 边界铁律：服务端不存会话）。

#### 10 · `GET .../events` SSE

见本文第 5 节。

### 3.3 错误码

| 码 | 含义 | 前端该怎么办 |
|---|---|---|
| **409** | `revision` 不匹配（别人先改了）| ⚠️ **不做合并**，提示「图已被修改」+ 重新加载（§5.3）|
| **422** | 状态机不允许这个转移 | 提示具体原因；这通常意味着界面上的按钮本不该出现 |
| **400** | 提交的结构校验不过 | 把返回的 `issues` 显示在问题条上 |
| **403** | 不是这张图的 owner | 退出 / 提示 |
| **401** | JWT 过期 | `api/client.ts` 统一拦截（§11 早就规划了）|

> ⚑ **409 处理是前端的头号难点**，也是 demo 里最缺的一块。
> §5.3 的原则是**遇到冲突让人决定，不替人猜** ——
> 所以是"提示 + 重载 + 让用户重做"，**不是自动重试**。

---

## 4. Java → Python

**不在本文范围。** 见 `DEV_DOC §3.4`（三个接口）和 §3.2（边界铁律）。

前端**永远不直接调 Python** —— Python 不暴露给公网，也不做鉴权。
这是 §3.1 那张图的物理含义。

---

## 5. SSE 事件形状

`§5.4` 定了四条**约定**，这里定**具体形状**。

### 约定（引用 §5.4）

| 约定 | 理由 |
|---|---|
| 连接时**先发 snapshot 再订阅** | 否则静默期连上的客户端盯着白屏 |
| 每条事件带**完整状态**，不是 delta | 重连不需要补发历史 |
| `event:` 名 = 事件自身 `type` 字段 | 客户端只按一个字段分发 |
| 终止类型（`completed`/`failed`/`cancelled`）**关闭流** | 明确生命周期 |

### 形状

```jsonc
event: snapshot
data: {
  "type": "snapshot",
  "mapId": "map_9f2c",
  "revision": 7,
  "status": "executing",
  "outline": [ /* 完整扁平数组 */ ],
  "issues": [...],
  "summary": { /* §5.2 */ }
}

event: updated
data: {
  "type": "updated",
  "revision": 8,
  "nodes": [ /* ⚑ 这次变动的节点的【完整对象】，不是字段 diff */ ],
  "issues": [...],
  "summary": { ... }
}

event: completed | failed | cancelled
data: { "type": "completed", "revision": 9, "summary": {...} }
```

### ⚠️ 一处我要标出来的歧义

§5.4 写的「**每条事件带完整状态**，不是 delta」，**两种读法都说得通**：

| 读法 | 含义 | 代价 |
|---|---|---|
| A. 整图 | 每条事件都带**整张图的 outline** | 200 个节点 × 每条事件，**很重** |
| B. 整节点 | 每条事件带**受影响节点的完整对象**（不是字段级 diff）| 轻，且客户端仍能按 revision 幂等应用 |

**本文按 B 写**（上框的形状就是 B），理由：

- §5.4 那句的**理由是"重连不需要补发历史"** —— 而重连由**第一条约定**
  （`snapshot`）已经解决了，不需要每条事件都全量
- B 仍然满足"不是 delta"的**字面要求** —— 节点对象是完整的
- 前端的渲染本来就是 `outline.map(n => n.id === e.node.id ? e : n)`，
  B 是天然的对应

> ⚠️ **这条要请 DEV_DOC 的维护者确认。** 如果本意是 A，
> 那前端的处理更简单（直接整份替换），但带宽和 GC 成本要重新评估。

### 前端要新增的

`lib/sse.ts`（§11 早就规划了）：**手写 SSE 解析** —— 因为
`EventSource` 带不了 `Authorization` 头，只能用 `fetch` + `ReadableStream` 自己解
（附录 A 的 C2）。

---

## 6. 前端要改哪里

**这一节是对 `ARCHITECTURE.md` 那句「将来 A0/A6 接上去，前端这一层不需要重写」的验证。**

| 文件 | 改什么 | 工作量 |
|---|---|---|
| `lib/layout.ts` | **不动** ✓ | —— |
| `lib/outline.ts` | **不动** ✓（`buildTree` / `rollupStatuses` / `displayState` / `hasBlockingIssue` 全部照用）| —— |
| `lib/outlineEdit.ts` | **不动** ✓ —— §5.3 说客户端负责算，它正是那个"算" | —— |
| `types/outline.ts` | **不动** ✓ —— 契约本来就是按 §4.2 写的 | —— |
| `features/canvas/*` | **几乎不动** ✓（渲染层只吃 `outline` + `issues`）| 小 |
| `features/chat/*` | **几乎不动** ✓（`summary` 从哪来都一样）| 小 |
| `lib/summary.ts` | ⚠️ 若 Java 返回 `summary` → 这个文件降级为**兜底/测试用** | 小 |
| `lib/sse.ts` | **新增** | 中 |
| `api/client.ts` | **新增**（单一请求入口：auth / 401 / 错误归一）| 中 |
| `api/editor/` | **新增**（结构性编辑 + 按节点串行保存队列 + 409 处理）| 大 |
| `App.tsx` | 三处 effect 换成网络 + SSE；删掉假延迟 | 大 |
| **`lib/simulation.ts`** | ⚠️ **整个报废** —— `advance` 归 Java，节点操作变成发请求 | —— |
| **`mocks/`** | ⚠️ **整个报废**（建议留作测试夹具，别删）| —— |

### 诚实的结论

**「前端不需要重写」这句话，大部分成立，但不是全部：**

| 判断 | 结论 |
|---|---|
| 渲染层、算法层、类型契约 | ✅ **完全成立** —— 一行不改 |
| 状态层（`simulation.ts`）| ❌ **整个报废** —— 它模拟的正是 Java 的职责 |
| 数据层（`mocks/`）| ❌ **整个报废** —— 它模拟的正是后端 |
| 编排层（`App.tsx`）| ⚠️ **要改** —— 但改的是"从哪拿数据"，不是"怎么渲染" |

> ⚑ **准确的说法应该是**：
> **"接口形状已经被钉死了，所以 A0/A6 接上去时不用改【怎么显示】，只需要改【从哪拿】。"**
>
> 这两句话的区别很重要 —— 前一句听着像"什么都不用做"，
> 会让 A6 排期时低估工作量。**`App.tsx` 的编排改造 + 新增的 `api/` 层是实打实的工作。**

---

## 7. 联调顺序

**原则：从只读到写入，每一步都能独立验证，且前一步是后一步的前提。**

| 步 | 接什么 | 为什么排这个位置 |
|---|---|---|
| **第 1 步** | `GET /maps/{id}` → 渲染 | **只读**。先把"真数据能渲染"验通，此时 `mocks/` 还留着做对照 |
| **第 2 步** | `POST /maps` → 规划 | 接上 A0。这一步之后 `pickDataset` 和假延迟可以删了 |
| **第 3 步** | 三个操作接口 | 单点写入 + `revision` 递增。**第一次遇到 409 就在这里** |
| **第 4 步** | SSE | 执行推进。接上之后 `advance()` 可以删了 |
| **第 5 步** | `PATCH /outline` | **最难的一步** —— 结构性编辑 + 串行保存队列 |

**第 5 步放最后**，因为它是唯一一个"客户端要算好再提交"的接口，
而且它是**唯一会大量触发 409 的地方**（拖拽是高频操作）。
前四步跑通之后，`revision` 的语义和 409 的处理都已经在真实流量里验过了。

---

## 8. 验证清单

### 每一步都要验的

- [ ] 请求带 `revision`，响应回新的 `revision`，客户端**存下来**
- [ ] 故意用一个过期 `revision` 发请求 → **必须收到 409**，且界面提示"图已被修改"
- [ ] 401（过期 token）被 `api/client.ts` 统一拦住，不在各处零散处理

### 第 1 步

- [ ] `mocks/` 换成真数据后，**渲染结果一致**（拿 japan 数据集对照）
- [ ] `issues` 里的 error 仍然触发 §7.1 的闸门

### 第 2 步

- [ ] 规划返回的 `outline` 能过 `buildTree()` —— **一条 issue 都没有**
- [ ] **阻断性问题时，「确认并开始执行」不出现**（前端的闸门）
- [ ] `POST /start` 在服务端**也**拒绝有阻断问题的图

### 第 3 步

- [ ] 批准 / 否决的后端结果与前端的 `displayState()` 显示**一致**
- [ ] 拒绝后：`assignee=user`、`status=todo`、`approval.status=rejected`
      （⚠️ 前端 `displayState` 有 `status==='todo'` 的护栏，别被它掩盖了真实数据的问题）
- [ ] 「全部批准（N）」是**一次请求**，不是 N 次
- [ ] 人工标记完成 → `locked=true`

### 第 4 步

- [ ] 连接时**先收到 snapshot**
- [ ] 终止事件后**流关闭**，重连不会拿到已结束的图
- [ ] 断网重连 → 拿到新 snapshot，状态**不丢**

### 第 5 步

- [ ] 拖拽后服务端校验通过，`revision` +1
- [ ] **连续快速拖拽** → 不产生竞态（串行保存队列生效）
- [ ] 客户端提交一个**故意算错的结构**（比如留个 order 空洞）
      → 服务端 **400 + issues**，不落库
- [ ] 删除一个被依赖的节点 → 下游 `depends_on` 被清理（§5.3）

---

## 附：本文没有回答的问题

诚实列出来，免得读的人以为覆盖全了：

| 问题 | 该谁定 |
|---|---|
| §5.4「每条事件带完整状态」到底指整图还是整节点 | 需 DEV_DOC 维护者确认（见本文第 5 节）|
| 建图的成本 / 超时（NFR-1 说 P95 < 15s）在接口上怎么体现 | A6 |
| 提案的 `operations` 具体是什么形状 | A0（§5.3 只说"操作列表"）|
| 「改名」（FR-6 剩下的最后一项）要不要单独接口 | 见本文 3.0 节 —— 它会是第四个接口 `PATCH .../nodes/{id}` |
| 多用户 / 权限边界（`ownership`）在接口上怎么表达 | §9.1 |
