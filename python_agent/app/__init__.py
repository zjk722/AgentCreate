"""Python Agent 引擎 —— §3.1 三层里的第三层。

⚠️ §3.2 边界铁律：这一层【完全无状态】。

    ❌ 不连数据库
    ❌ 不存会话 / 不存历史
    ❌ 不知道 revision 是什么
    ❌ 不决定"要不要批准"（那是 Host 的事）

这不是洁癖 —— 它用网络边界【物理性地】保证了「模型不能直接写库」。
容器里没有 DATABASE_URL，也没装任何数据库驱动，想连也连不上。
"""
