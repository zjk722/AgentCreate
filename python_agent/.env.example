# 复制成 .env 再填真值：cp .env.example .env
# ⚑ .env 已被 .gitignore 挡住，不会进仓库；这个 example 文件会进（.gitignore 里那条 !.env.example）。

# DeepSeek —— 见 DEV_DOC §1.5 的技术栈与 §11 的 llm/client.py
DEEPSEEK_API_KEY=

# 下面两个有默认值，一般不用写
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
