# 变更日志

> **仓库** https://github.com/zjk722/AgentCreate
> **记录范围** 从仓库初始化到 2026-09-15 的全部推送
> **作者** zjk722

**提交粒度的约定**：**设计决策** 与 **代码** 分开提 —— 这样 `git log` 能一眼看出
"哪些提交改的是想法、哪些改的是实现"。同理，文档与功能也分开。

---

## 概览

| 推送 | 时间 | 范围 | 提交数 | 主题 |
|---|---|---|---|---|
| **1** | 12:49 | `028c5f5` | 1 | 仓库初始化 |
| **2** | 14:00 | `028c5f5..95b9f55` | 2 | 设计裁决回写 + D1 脚手架 |
| **3** | 14:09 | `95b9f55..282b2e4` | 1 | D2 数据层 |
| **4** | 19:39 | `282b2e4..3d09043` | 7 | D3–D4 渲染 → 对话流程 → 失败处置 |
| **5** | 16:31 | `fc6ab3f..dad9bc2` | 1 | 任务图入场动画 |
| **6** | 18:17 | `dad9bc2..ddb2ce5` | 3 | 结构性编辑 + 依赖图 + §7.1 闸门 |
| **7** | 09-13 19:15 | `b8ec82d` | 1 | 前端 ↔ Java 接口契约（**补记**）|
| **8** | 09-14 20:54 | `c3d5867..d9c6511` | 2 | 工具注册表 + A0 骨架 |
| **9** | 09-15 21:12 | `40db3cb` | 1 | A0 管道接通 + §7.2 ① 生成期校验 |

**累计**：23 个提交 · 75 个文件 · **+15181 / −21** · 测试：前端 279 · **Python 37**

> 行内的提交数只统计**代码 / 文档改动**；本文件自身的提交不占行，但已计入累计。
> 推送 1–6 都发生在 09-12，所以那几行只写了时间；7 起跨天了，带上日期。
>
> ⚑ **累计的口径本轮改了。** 推送 6 时这里记的是「约 13500 行新增」，
> 而用 `git diff --shortstat <初始提交>..<该次记录时的 HEAD>` 复算，当时只有 **12251** ——
> 对不上，且那个数字怎么来的已不可考（大概是估的）。本轮起一律用上面这条**可复现**的命令。
> 推送 1–6 各自那行的「规模」没回算，暂时保留原样。

---

# 推送 1 · 12:49 · 仓库初始化

```
028c5f5  chore: 初始化仓库，纳入项目开发文档
```

| | |
|---|---|
| 文件 | 3 个（DEV_DOC.md / .gitignore / .gitattributes）|
| 规模 | +1124 |

**做了什么**

- 纳入 `DEV_DOC.md`（Agent 任务图引擎 v0.2）
- `.gitignore`：覆盖 Python / Java / Node 三类产物；**排除 `.claude/skills/`**
  —— 那是 uipro 装的 172 个第三方模板，一条命令即可复现，不该进版本库
- `.gitattributes`：统一 LF 行尾。**理由是 §11 有 Makefile 和 docker-compose，
  项目最终跑在 Linux 容器里**，CRLF 的 shell 脚本进去会变成 `\r: command not found`

---

# 推送 2 · 14:00 · 设计裁决 + 脚手架

```
93c95fb  docs: DEV_DOC v0.3 —— 前端 demo 设计评审的 9 处裁决
95b9f55  feat(frontend): D1 脚手架 —— Vite + React 19 + TS + Tailwind 两栏骨架
```

## `93c95fb` 设计裁决（+140 / −17）

在做前端 demo 之前先把 9 处文档矛盾裁决并回写。

**为什么先做这个**：文档 §0 写着「照着这份文档写，不要自由发挥」——
不回写，下次读文档就会按旧的矛盾版本走一遍。

| # | 位置 | 裁决 |
|---|---|---|
| 1 | §4.2 | 节点加 `approval` 字段（A6 由 Java join `approvals` 表拍平）|
| 2 | §4.2 | `order` 必须连续 `0..k-1` |
| 3 | §5.2 | 状态机补 `rejected` 转移边（→ `assignee=user`, `status=todo`）|
| 4 | §5.2 | **修正：`blocked` 是 assignee，不是 status** |
| 5 | §5.2 | `planned`/`assigned`/`pending` 是入库前内存态，UI 见 `todo` |
| 6 | §5.3 | 补拖拽两个手势（改 `parent_id` 调层级 / 改 `order` 调顺序）|
| 7 | §5.3 | 并发冲突一律 `409`，**不做合并** |
| 8 | §7.2 | 校验按「生成期/读取期/依赖图」三组重排；拆 `E_CYCLE` 并补 4 个新 code |
| 9 | §11 | 澄清 `lib/outline.ts` 与 Python `outline.py` **不是同一算法** |

**最有价值的一条**：第 4 条 —— §5.2 的状态机图把 `blocked` 画成了状态，
但 §4.2 和 §1.2 的图示都表明它是 `assignee`。**证据是 §1.2 把 `blocked`
和 `agent`/`user` 放在同一位置**，而 `status` 在另一处。

**第 7 条的原则**（后来反复用到）：

> **遇到冲突，让人决定，不要替人猜。** 合并是在猜用户意图（猜错了还不告诉任何人）；
> `409` 是把冲突摆到用户面前让他重做。

## `95b9f55` D1 脚手架（+2225，15 文件）

- create-vite 9 模板：React 19.2 / Vite 8.3 / TS 6.0 / oxlint
- Tailwind **v4**：走 `@tailwindcss/vite` 插件，CSS-first 配置，**没有 `tailwind.config.js`**
- 两栏布局：左 380px 目标输入，右画布（点阵背景）
- 应用 `h-screen + overflow-hidden`，滚动只发生在画布内
  —— **为 D5 的缩放/平移预留**，避免与页面级滚动打架

---

# 推送 3 · 14:09 · D2 数据层

```
282b2e4  feat(frontend): D2 数据层 —— 类型契约 + mock 数据集 + 扁平转树 + 单测
```

| | |
|---|---|
| 文件 | 11 个 |
| 规模 | +1247 |
| 测试 | 0 → **25** |

**三个交付**

**① `types/outline.ts` —— 契约**
严格按 §4.2。注释里写明**三轴正交**（`assignee` / `status` / `approval`），
以及为什么不能压进一个字段。

**② `lib/outline.ts` —— 扁平数组 → 树**

这是 D2 的核心。一半代码在处理**坏数据**：

| code | 检查 |
|---|---|
| `E_DUPLICATE_ID` | `Map` 建索引会静默覆盖 —— **不报错，只是莫名少一个节点** |
| `E_ORPHAN_PARENT` | `parent_id` 指向不存在的节点 |
| `E_CYCLE_PARENT` | `parent_id` 成环 |
| `W_ORDER_INVALID` | 兄弟 `order` 序列 ≠ `[0..k-1]` |

**一个关键简化**：`parent_id` 是**单指针**，所以环检测只需"往上走"——
不需要 DFS 三色标记，也不需要拓扑排序（那是 `depends_on` 那种任意图才需要的）。

**一条原则**：

> **对用户数据，宁可显式显示异常，也不要静默丢弃。**
> 静默丢弃会让用户看到"内容凭空消失，且无从知道发生过什么"。

**③ `mocks/` —— 方案 B：数据源 = §8.3 评测集种子**

| 数据集 | 内容 |
|---|---|
| `japan-trip` | 种子 #2 · 任务型，覆盖全部 5 status × 3 assignee × 4 审批态 |
| `ml-knowledge` | 种子 #1 · 知识型，深度 3 单根全绿 |
| `vague-mood` | 种子 #10 🔴 退化边界：只出根节点，**不编造** |
| `corrupt-sample` | 非种子 · 坏数据样本，触发全部 4 类读取期问题 |

**一份数据两用**：现在渲染 demo，将来 A1 做评测时同一批种子既能跑断言也能肉眼看。

**测试抓到的第一个 bug**：`x` 存的是节点**中心**，却让深度 0 的节点中心只加了
`padding` —— 盒子左半边挂到画布外去了（左边缘 = 20 − 50 = −30）。
补了"最外侧节点不能挂出画布"的回归断言。

---

# 推送 4 · 19:39 · 渲染 → 对话 → 失败处置

```
64b9340  feat(frontend): D3+D4 布局与渲染 —— 页面现在能看见任务图
ec653ae  feat: 补 result_summary —— 引擎产出了结果却无处可存（文档真缺口）
465ceb1  feat: assignee_reason 枚举 + summarize() —— 对话汇报的数据层
5cf90d7  feat(frontend): 对话区 + 模拟执行 —— 先议定 plan，再开始任务
28b0409  feat(frontend): 逐项操作 + 画布节点可改状态
0d644e2  docs: 新增 ARCHITECTURE.md —— 跨章节的全景与导航
3d09043  feat: 失败处置 + 容器状态汇总 —— 根节点不再永远停在 running
```

## `64b9340` D3 布局 + D4 渲染（+1401 / −113）

**D3 · ADR-6 点名的"最大成本"的前端唯一实现**

核心是**缩放**：`d3.tree()` 给的 `x`/`y` **不是像素**。

| d3 字段 | 真实含义 |
|---|---|
| `d.y` | 深度，整数 `0,1,2...` |
| `d.x` | 该层内的位置，单位是"节点间距" |

**左右布局 vs 上下布局 = 这两行互换**：

```
屏幕 X = d.y * (nodeWidth + gapX)
屏幕 Y = d.x * (nodeHeight + gapY)
```

**D4 · 三条视觉通道**（这是本项目的核心视觉设计）

| 信息 | 通道 | 为什么 |
|---|---|---|
| **status** | 颜色 **+ 图标形状** | `done`(✓) 和 `failed`(✕) 是典型红绿配对，约 8% 男性分不出 —— 形状必须区别够大 |
| **assignee** | 边框**线型**（实线/虚线/点线）| 和颜色完全独立。若两者都用颜色，看到红色虚线卡得先猜"红的是状态还是归属" |
| **approval** | 独立文字 chip | 审批的"需二次确认"和执行的"失败"是两件事，都用红会误导 |

**另一个教训**：Tailwind 是**扫描源码文本**决定生成哪些样式的。
写 `` `text-${status}` `` 这种拼接，扫描器看不到完整类名，样式不会被打包 ——
**而且不报错，只是颜色莫名失效**。所以映射表必须写成字面量的完整类名。

## `ec653ae` 补 `result_summary`（+144 / −17）· **文档真缺口**

**问题**：§3.4 的 `/v1/execute_task` 一直返回 `result_summary`，
但 §4.2 的 `outline` **没有字段接它** —— 引擎产出了结果却无处可存。
用户盯着任务图最想知道的那件事（Agent 查到了什么）在界面上不可见。

**设计取舍**：摘要进节点，全文进审计

| | 放哪 | 理由 |
|---|---|---|
| `result_summary` | 节点（JSONB）| 短、跟节点走、不需单独查询 |
| 完整输出 | 审计日志 | 由 `evidence.result_ref` 指向（§5.2 已有的指针模式）|

**若把完整输出塞进节点**，200 节点会把 outline 撑爆，
而 §4.3 要求它是「写入原子、整批替换」的字段。

## `465ceb1` `assignee_reason` 枚举（+625）· **第二个文档缺口**

**问题**：§1.2 明写着分配原因（`[user · 需本人办理]`、`[blocked · 无可用工具]`），
但 §4.2 没有字段承接 —— 界面上要显示的这句话在存储层无处可放。

**关键决策：用枚举，不用 `reason: string`**

| `assignee_reason` | 含义 | 规则出处 |
|---|---|---|
| `needs_human` | 只能人来 | §1.2 |
| `no_tool` | 没有能做的工具 | §9.2 |
| `agent_failed` | Agent 失败转人工 | §5.2 |
| `user_rejected` | 用户否决 | §5.2 |
| `policy_denied` | Policy 拒绝 | §9.2 |

> **自由文本是模型的自我报告，不可验证；枚举是规则产出，可验证。**
> 让模型自由写，它会产出「因为签证政策复杂」这种听着合理但**无出处**的句子。

界面那句人话由前端从枚举**翻译**（`lib/reasons.ts`），措辞可改而不动数据。

## `5cf90d7` 对话区 + 模拟执行（+1123 / −102）

**流程改了**：不再是"输入 → 直接出图"，而是 §5.1 那条链：

```
输入目标 → ① 规划 → ② 裁决 → ③ 用户确认 → ④ 展开执行
```

**`lib/simulation.ts` 按 §5.2 + §9.2 推进，不是硬涂绿**

推进的规则：`running → done`（挂证据）；依赖就绪的 `todo → running`（受 Semaphore(6)）。

**拒绝推进**的四条：审批 `pending` / `assignee≠agent` / 容器节点 / 依赖未满足。

**探出的真实现象**：日本那份数据集按规则**几乎跑不动** ——
两个 agent 任务卡在等审批，一个依赖「决定出行日期」（人做的）。
**这不是 bug，是正确的产品行为。** 界面如实说"卡在哪、要你做什么"。

**一个设计细节**：只有用户解开阻塞，Agent 才继续 ——
不是定时器自己往前跑。**那会让"审批"看起来毫无意义。**

**测试推翻了我的一个设计**：我原本让"本 tick 完成的节点要等下 tick 才解锁下游"，
还在注释里写成了有意设计。测试写出来后重新想：**那是错的** ——
白等一轮与 ADR-5「调度器拿到结果当场决定下一步」不符。改成读实时状态。

## `28b0409` 逐项操作 + 画布节点可改状态（+358 / −45）

两个改动指向同一件事：**用户在哪儿看到问题，就能在哪儿解决它。**

- 对话区待办支持逐条操作，不再只能全选
- 「等你点头」每条同时给**批准和否决** —— *"只能同意"不是审批，是通知*
- 画布节点点开后，详情面板顶部出现可用操作（与对话区共用同一套语义）

**一条设计约束**：手动完成**只开放给 `user` / `blocked` 的节点**。
Agent 该做的任务不能被人顶掉 ——
否则"Agent 有没有真做完"这个最关键的信息就被污染了。

**顺带修了文档 v0.5**：§7.3 的 `E_MISSING_EVIDENCE` 此前**没写作用域**。
它存在的理由是「不信任模型的自我报告」，而**人勾的完成不是模型的自我报告**。
不加限定，用户勾掉的每项都会被误报成"缺少证据链" ——
**报警疲劳一旦形成，真正的假完成就没人看了。**

## `0d644e2` ARCHITECTURE.md（+360）

DEV_DOC 是权威但按主题分章，经过 v0.3–v0.5 三次修订后，
想回看"整体长什么样"需要交叉阅读。

**这份文档刻意不复制细节**，只做导航：全景图 / 数据流 / 进度 / 约束清单。
凡有细则一律给章节号。

> **理由就写在同一页**：导航层若复制细节，两者必然漂移 ——
> 这正是本项目反模式里"镜像双后端必然漂移"的同一个道理，**只是发生在文档层**。

## `3d09043` 失败处置 + 容器汇总（+482 / −47）

**这两件事都来自同一个问题**：「准备一次日本关西七日游」的完成标准是什么。

### 一 · 失败处置 —— 文档没写的那条分支

§5.2 的降级表规定了「参数错误 → 转 user」「无工具 → blocked」，
但**"重试也失败之后呢"文档没写**。

**停下来让人决定是唯一诚实的做法** —— 系统不该替用户判断"这件事还值不值得做"。

| 用户选择 | 结果 |
|---|---|
| 我来处理 | `assignee=user`, `status=todo`, `reason=agent_failed` |
| 不处理 | `assignee=user`, `status=skipped`, `reason=agent_failed` |

对话区新增一组「**需要你决定**」，排在所有组最前 —— 它是唯一需要**判断**的一组。

### 二 · 容器状态汇总（派生，不存储）

```
failed > running > 全 done/skipped > 全 todo > 其余为 running
```

**为什么 `failed` 压过 `running`**：下面的还在跑、上面有一个失败了，
显示 `running` 会让人以为一切正常。**把异常顶到眼前是本项目的产品立场。**

**为什么派生而不存储**：存下来会漂移（父说 `done` 而子任务没做完）。
派生则永远不会不一致。

**根节点的完成标准由此确定**：

> **所有后代叶子都是 `done`，或"被人有意放弃的 `skipped`"。**
> 只要还有一件没办、还在办、或失败了没人管，根节点就不是已完成。

### 三 · 顺带修掉的两个真 bug

**Bug 1 · 级联跳过的时机错了**

原本「依赖 `failed` → 下游立即 `skipped`」。但 `failed` 意味着**还没被决定** ——
此时级联等于**提前替用户放弃**，而且不可逆。

改成：上游 `failed` 时下游等着，用户决定放弃了才级联。

**这正是最初那个问题"为什么打印行程单默认跳过"的根因** ——
它原本写死 `skipped` 却**没有依赖**，按 §5.2 不可能被级联跳过。
改成 `depends_on` 购买旅行保险 + 初始 `todo`，两个状态终于成了因果。

**Bug 2 · 容器被当成了任务**

「预订安排」因 `failed` 的子节点汇总成 `failed`，于是出现在了「需要你决定」里。
**但分组不是任务 —— 你没法"做"一个分组。**

`summarize` 现在从扁平数组算出容器集合（任何被当作 `parent_id` 的节点），
四个可操作分组都排除它们；`byStatus` / `byAssignee` 仍统计全部。

---

# 推送 5 · 16:31 · 任务图入场动画

```
dad9bc2  feat(frontend): 任务图入场动画 —— 节点按层级长出，不再凭空出现
```

| | |
|---|---|
| 文件 | 5 个（全部在前端）|
| 规模 | +284 / −7 |
| 测试 | 137 → **141** |

## 问题

规划结束后 `setOutline` 把整份 `outline` 一次性塞进去 —— 14 个节点
「唰」地全冒出来，像凭空变出来的。

## 做法 · 两条动画

| | 起始态 | 时长 |
|---|---|---|
| **节点** | 朝父节点方向（左）偏 10px + 缩到 94% + 透明 | 320ms |
| **连线** | 长度 0（`stroke-dashoffset: 1`）| 240ms |

**每层错开 90ms。** 为什么按【层级】而不是按节点序号：一层 = Agent 的一次拆解，
层内是这一次拆解的产出。按序号错开会把本来整整齐齐的层级搅成一串随机气泡。

**根节点单独一套 keyframes** —— 它没有父节点可"长出来"，所以不往任何方向偏，
只做淡入 + 放大。统一写成"从下方浮起"是错的：在这个左右布局里，
**下方不代表任何东西**。

**连线比节点短 80ms** —— 线先到位、节点再落上去。反过来会看到卡片悬在
一根还没连到它的线前面。

## 四条硬约束

| # | 约束 | 为什么 |
|---|---|---|
| ① | 只动 `transform` / `opacity`，不碰 `left` / `top` | 位置由内联 `left/top` 钉死；动画改这两个会让浏览器**每帧重排**（重算谁在哪儿、谁挡着谁）。`transform`/`opacity` 不引起重排 |
| ② | 元素的**静态样式就是最终态** | 反过来写（默认透明、靠动画显形）的话，动画一失效元素就**永久隐形**。现在最坏只是"没有动画" |
| ③ | `animation-fill-mode: backwards` | 延迟期间保持起始态。少了它，元素会先闪一下再开始动 |
| ④ | `prefers-reduced-motion` 降级**不等于完全不动** | 那等于白做。去掉位移 / 缩放 / 延迟，只留 200ms 淡入 ——「不是凭空出现」在最严格的设置下依然成立，同时避开前庭不适 |

## 两个不显眼、但写错就真出问题的地方

**① 延迟走 `--enter-delay` 自定义属性，不走内联 `animation-delay`**

浏览器的规矩是：**写在标签上的内联样式，优先级高于样式表里的任何规则**。
延迟必须按运行时数据（第几层）算，只能用内联样式；但约束 ④ 的降级规则
又要把它清零 —— 直接内联写 `animation-delay` 的话，降级会被它盖掉，
**延迟根本清不掉**。换成自定义属性后，降级规则改的是 `animation-delay`
本身，内联的 `--enter-delay` 只是没人读，两边互不打架。

**② `pathLength={1}` 把曲线长度归一化成 1**

这样 `stroke-dasharray: 1` 就直接表示"整条线"，不必去问 `getTotalLength()`。
**读 DOM 的代价很大** —— 浏览器为了给出准确答案，会立刻停下手上所有的活、
把整页位置重新算一遍。十几个节点各读一次，就是十几次强制重算。

## 测试 +4，且做过变异验证

| 测什么 | 为什么值得 |
|---|---|
| 根节点用不位移变体 | ⚠️ 这条**不能**用 `toContain('node-enter')` 断言 —— `'node-enter-root'` 本身包含 `'node-enter'`，那个断言恒真、**等于没测**。改成计数 |
| 等待时间 = 层 × 90ms | 0 / 90 / 180 各断言一次 |
| 连线的延迟跟**子**节点走 | 最容易写反的一处 |
| `pathLength` 归一化没被误删 | 少了它描线会变成点线，**而且不报错** |

> **为什么要做变异验证**：测试通过 ≠ 测试有用。写错的测试会永远通过 ——
> 它比没有测试更糟，因为它给的是**虚假的安全感**。
> 这次把 `to.depth` 故意改成 `from.depth` 跑了一遍，
> 确认测试确实变红（`expected 1 to be 2`），再改回来。

**验证**：141 passed（+4）· `tsc` 通过 · `oxlint` 无告警 · `build` 通过

---

# 推送 6 · 18:17 · 结构性编辑 + 依赖图 + 闸门

```
ccbef54  fix(frontend): 点「坏数据样本」出来的是日本旅游
34b0e1f  feat(frontend): 结构性编辑（拖拽 + 删除）+ 依赖图（渲染 + 校验）+ §7.1 闸门
ddb2ce5  docs: DEV_DOC v0.6 + ARCHITECTURE 同步
```

| | |
|---|---|
| 文件 | 26 个（其中 9 个新增）|
| 规模 | +3611 |
| 测试 | 141 → **279** |

**这一批只有一个主题：让这张图说真话。**

图是用户判断"Agent 会不会做错"的**唯一依据**。它一旦不可信，
这个产品的核心价值就没了 —— 下面几件事都是沿这条线。

## 一 · 节点拖拽（FR-6 的"调层级 / 调顺序"）

§5.3 的两个手势：拖到卡片**中间 50%** 成为它的孩子，
拖到**上/下 25%** 插到它前面/后面当兄弟。

⚠️ 文档写的是"拖到缝隙"，而缝隙只有 16px —— 鼠标瞄不准，
也低于无障碍的 24×24 最小指针目标。切成上中下三段之后，
可落点从一条缝扩大到整张卡的四分之一，**语义完全一样**。

**五条护栏**（`lib/outlineEdit.ts`，纯函数、可单测）：`self` /
`descendant`（成环）/ `multiple_roots` / `noop` / `not_found`。

> ⚑ **为什么 `noop` 必须拦**：真实系统里任何一次写入都会 `revision` +1，
> 于是别人基于旧版本的编辑全部作废。
> **为一次零改动的操作去作废别人的工作，是纯粹的伤害。**

> ⚑ 落点高亮**直接调用真正的移动逻辑**来判定，不另写一套 ——
> 两套规则迟早会不一致：用户看到"可以放"的绿光，松手却没反应，
> 而且无从知道为什么。

**顺带做了「不用拖也能改」的入口**（详情面板下拉选父亲 + 上移/下移）。
理由是无障碍规范（WCAG 2.2 AA 的 `dragging-alternative`）：
**凡是能拖的，都必须另有一条不用拖也能走的路。**
而且它不只是给少数人的补丁 —— 在两个下拉选项里挑一个，
比用鼠标把卡片拖进 16px 的缝，对所有人都更好用。

## 二 · 依赖连线：把"执行顺序"画出来

起因是一个真问题：**拖拽改不了执行顺序，但图让人以为能。**

`parent_id` / `order` 是"图长什么样"，`depends_on` 才是"谁必须先做"
（§5.2 的拓扑排序只看后者）。用户把「支付」拖到「下订单」上面，
执行顺序一点没变 —— **图在骗人。**

现在画两种线，一眼可分：

| 线 | 外观 | 说什么 |
|---|---|---|
| 父子线 | 实线、无箭头 | 归在哪一类 |
| 依赖线 | 虚线、带箭头 | 谁必须先做完（从**先做的**指向**要等的**）|

⚠️ **实测发现 japan 数据集 3 条依赖里有 2 条从别的卡片身上穿过去。**
它们连的是同列兄弟、中间夹着卡片，而"拱一下"最多拱半个卡片宽（84px），
绕不开。改成走卡片左边的空隙（层与层之间天然留着的走廊）。

绕道用**实打实的线段拐角**而不是贝塞尔 —— **曲线碰不到控制点**，
你以为拐到旁边了，其实只拐了一部分，还是压在卡片上。

## 三 · 依赖图校验：§7.2 第 ③ 组从零补上

文档里第 ③ 组（`E_CYCLE_DEP` / `E_DANGLING_DEP`）**定义了但从未实现**。

⚠️ 这两个错误都会让调度器**静默**停住：

| code | 表现 |
|---|---|
| `E_DANGLING_DEP` | 依赖悬空 → 那句 `byId.get(d)?.status === 'done'` 求值成 false → 那个任务**永远等着**，而界面上它就是个普普通通的「待办」 |
| `E_CYCLE_DEP` | 环上的任务互相等对方先做完，谁都不会先跑 |

⚠️ 用 DFS 必须**三色**（白/灰/黑），不能只用"走过 / 没走过"两色 ——
两色会把**菱形结构**（两条路通向同一个节点，比如"订机票"和"订酒店"
都依赖"决定日期"）误判成环。**japan 数据集里正好有这种结构。**

## 四 · §7.1 的闸门从来没接上

`hasBlockingIssue()` 此前只被 `summary.ts` 用来**算汇报的语气**，
没有任何地方拿它挡过什么 —— 顶上弹着"7 个错误（阻断）"，
底下的「确认并开始执行」照样能点。

而且 `advance()` 遍历的是整个 `outline`（不管节点在不在树上），
**所以游离节点会被真的执行。**

现在两道：`AgentMessage` 不给按钮（并说明原因）+ `confirm()` 自己拒绝。

> ⚠️ 闸门不能只活在调用方那行 JSX 里 —— **那个位置没有测试守着。**

## 五 · 游离节点：告示牌立了，路也得给

原来游离节点区是光秃秃的 `<li>`，**点不开**。用户被告知"这些坏了"，
手里却没有任何工具去修。

现在每项可点，点开走同一个详情面板，用「调整位置」挂回树上。
（把环上某个节点挂回主树，**一次修两个** —— 另一个的父就是它。）

另外只有连得到根的节点才有资格当新父亲 —— 否则只是换个地方游离。

**顺带**把面板从 `absolute` 改成 `fixed`：它原来跟着内容滚动，
用户滚到页面底下点游离节点，面板会出现在**屏幕外面**，
表现就是"点了没反应"。

## 六 · 删除节点（FR-6 的"删"）

FR-6 要求"增删"，此前只做了拖拽。

两种方式**由用户选**（不替人猜）：一起删掉 / 子节点上移一层
（顶到被删节点**原来的位置**，不是排到最后）。

⚠️ 根节点 + 多个孩子时，"上移"会把图裂成两棵（`E_MULTIPLE_ROOTS`）——
那个选项**根本不出现**，而不是灰着（灰着的用户会反复点，想着"为什么不能"）。

> ⚑ **真正的工作量不在"把它从数组里拿掉"**（那是一行 `filter`），
> 而在两条收尾：
>
> | # | 收尾 | 不做会怎样 |
> |---|---|---|
> | 1 | **清理引用** —— 别人 `depends_on` 里写着它的得跟着删掉 | 立刻造出 `E_DANGLING_DEP`。**这是删除唯一的真风险** |
> | 2 | 同组重编号 | 留下 `order` 空洞 |

界面上**把后果摆出来再让确认**（连坐几个、有谁的依赖会断）——
这和 §5.3 给 AI 改稿定的「提案 / 应用分离」是同一条道理：
**人自己动手删的时候，凭什么不用看后果。**

## 七 · 顺带修掉的一个真 bug

**`moveNode` 重编号时没按 id 去重**（**测试抓出来的**）。

数据里带重复 id 时，重复的那条会占掉一个编号位，而它又不在树上 ——
于是真正在树上的兄弟被编出一个空洞。

> ⚑ **那个空洞是我们自己造的**：用户只是拖了一下，图上却多了一个错。
> **数据本来就有病，不是我们让它更病的理由。**

另外 `corrupt-sample` 从 4 个坑扩到 **6 个**（补上依赖悬空 + 依赖环），
两种各用**独立的新节点** —— 一个节点同时有两三种毛病的话，
界面上看不出哪条对应哪个问题，这份样本就失去了教学作用。

## 八 · 验证

`tsc -b` 通过 · `oxlint` 无告警 · `build` 通过 · **279 个测试**

⚠️ 关键逻辑都做了**变异验证**（把代码改坏，确认测试真的会红）：

| 改坏什么 | 结果 |
|---|---|
| 去掉"多根"护栏 | 3 条红，**指名道姓**报出是哪一步 |
| 三色退化成两色 | 5 条红，含真实数据的菱形结构 |
| 去掉依赖清理 | 5 条红，报的正是 `E_DANGLING_DEP` |
| 去掉闸门条件 | 1 条红 |
| 依赖线不绕道 | 3 条红，报出"从卡片 X 身上穿过去了" |

> **为什么值得做这一步**：测试通过 ≠ 测试有用。
> 写错的测试会永远通过 —— 它比没有测试更糟，因为它给的是**虚假的安全感**。
> 这一批里我就写出过一条永远为真的断言（见下面"两次自我纠正"）。

## 九 · 两次自我纠正（都是"静默"）

**① 一条永远为真的测试。** 我断言"转折点的坐标出现在路径字符串里" ——
而**贝塞尔的控制点也写在路径字符串里**，所以曲线就算一点没拐，
那条断言照样绿。改成把曲线**采样成点列**、逐个问"落在哪张卡片里"。

**② 一个什么都不做的类型检查。** 我一直用 `npx tsc --noEmit` 报"类型检查通过"——
而这个项目里它**检查 0 个文件、永远通过**
（根 `tsconfig.json` 是 `files: []` + project references）。
真正的检查是 `tsc -b` / `npm run build`。

> 两次都是**同一类**：一个不报错、只是什么都不做的东西。
> 和 #13（静默失败）是一个东西，只不过一次发生在测试里、
> 一次发生在工具链上。

---

# 推送 7 · 09-13 19:15 · 前端 ↔ Java 接口契约

```
b8ec82d  docs: 新增 INTEGRATION.md —— 前端 ↔ Java 的接口契约
```

| | |
|---|---|
| 文件 | 2 个（`INTEGRATION.md` 新增 + `ARCHITECTURE.md` 加指针）|
| 规模 | +570 / −4 |

> ⚑ **这笔是补记。** 它提交于推送 6 的记录之后，当时没记进本文件。
> 写这一节时顺手核了一遍 `ddb2ce5..HEAD` 的四个提交：只有这一个漏了，其余都有条目。

**为什么要单独写一份**

`DEV_DOC §3.4` 把 **Java → Python** 三个接口写得很细，而 **前端 → Java 一个 endpoint 都没有** ——
§3.1 的图里只有一行 `│ /api/v1/** (JWT Bearer) │`。
所以这篇不是"记录现状"，是**产出那层缺失的定义**。

**内容**

- 前端现在有 **9 处是假的**，每一行都是一个待接的接口
  （`pickDataset` **猜**数据集、700ms 假延迟、`advance()` 本地推进……）
- **10 个接口** + 错误码表。其中 **409 是前端的头号难点**，也是 demo 里最缺的一块
- **为什么不是一个 `action` 接口** —— 全文最值钱的一段：把 approve/reject/complete
  压进一个字符串字段，和 §4.2「三轴不要合并」是**同一条原则**。
  接口划分是第一道网（让非法状态**无法表达**），状态机校验是第二道
- **SSE 事件的具体形状**（§5.4 只定了约定），并把「每条事件带完整状态」到底是
  **整图**还是**整节点**这个歧义**标了出来**，请 DEV_DOC 维护者确认
- 诚实的结论：「前端不需要重写」大部分成立，但 `lib/simulation.ts` 与 `mocks/`
  **整个报废** —— `App.tsx` 的编排改造 + 新增 `api/` 层是实打实的工作

---

# 推送 8 · 09-14 20:54 · 工具注册表 + A0 骨架

```
c3d5867  feat(shared): 工具注册表 —— Java Policy 与 Python 引擎共读的单一真源
d9c6511  feat(python_agent): A0 骨架 —— make plan 跑通（占位数据，会自报家门）
```

| | |
|---|---|
| 文件 | 8 个 |
| 规模 | +473 |

**这一批的主题：把 A0 的【输入】准备好，再把管道接通。**

## 一 · 为什么先做工具清单，而不是先写代码

`/v1/plan` 的请求体里**必须带工具清单**（§3.4），而这份清单是
**Java Policy 和 Python 引擎共读的同一份**（§4.4）。
没有它，规划连"有哪些工具可用"都答不出来 —— 它是 A0 的**输入**，不是 A0 的产物。

反过来先写 Prompt，工具清单一定会变，**Prompt 就得写两遍**。

## 二 · ⚑ 填这张表撞出了 7 处文档缺口

这一批**没有改任何文档** —— 全部攒成一次 v0.7 回写。
7 处性质完全一样：**填一张表 / 写一段逻辑时才撞得上，读文档看不出来**
（与 v0.3–v0.6 那 6 处同源）。

| # | 缺口 | 缺在哪 |
|---|---|---|
| 1 | **§9.2 说「无工具 → `user`」** | 与 §1.2 / §4.2 / §5.2 三处的 `blocked` 冲突（3:1，建议改 §9.2；那行还会让 `no_tool` 枚举永远用不上）|
| 2 | **`policy_denied` 有取值、无规则** | §4.2 标的出处 §9.2 只是把"Policy 拒绝"重写了一遍（循环引用）|
| 3 | **`proposed_tool` 无处可存** | §3.4 的响应里有、§4.2 的 `outline` 没有 → §7.3 的 `E_ASSIGNED_WITHOUT_TOOL` 读取期执行不了 |
| 4 | **`evidence.result_ref` 无表可指** | §4.2 说"完整输出在审计记录里"，而 §4.1 的五张表里没有工具调用记录表 |
| 5 | **`blocked` 没有"我不做"的出口** | 三个接口没有一条能让它变 `skipped`；要加就得新增接口 |
| 6 | **审批可能被绕过** | 引擎执行时若换了 `side_effect` **更高**的工具，没有任何规则要求重新审批 |
| 7 | **幂等键含整图 `revision`** | 必须"首次尝试算一次、重试复用"；否则执行中一次无关编辑就让重试换个新键，去重**静默**失效 |

> ⚑ 第 4 条如今**有需求驱动**了：讨论中提出「点开节点能看 Agent 对这个节点做了什么」——
> 那正是这张表存在的理由。
> 而它必须区分两种文本：**工具调用的原始记录**（事实，要做）与
> **模型自述的执行过程**（自我报告，**不做** —— v0.4 已决定不存）。
> 一段读起来越顺的解释，越应该警惕它。

## 三 · 两处当场定下的决策

**① 证据链只管「外部动作」，不管「内部生成」。** 判据是「**用户能不能自己检验**」：

```
查机票        → 用户看不到，只能信 Agent 说 → 必须有证据
整理知识体系  → 内容就摆在节点上，读一眼就知道对不对 → 不需要"它真写过"的背书
```

不这么划，`ml-knowledge`（种子 #1，🟢 档"必须过"）那 17 个没证据的 `done`
会**全被判成 failed**；而反过来硬给每个知识节点配一次检索，
那个 evidence 就是**伪证据**（它指向的仍是模型自己的输出，
而 ADR-3 存在的全部意义就是不信任这个）。

连带要改 4 处：`E_MISSING_EVIDENCE` 加作用域（**同 v0.5 给它加 `assignee=agent` 那次**）、
`E_ASSIGNED_WITHOUT_TOOL` 加作用域、§8.2 的 `evidence_completeness` 分母改成"外部动作类"、
§4.2 的 `done` 说明与 D4 视觉通道各加一句（**两类绿勾要能看出区别，否则就是 #13**）。
容器节点**不用加字段** —— 前端 `summarize` 已用"有没有被人当过 `parent_id`"推出来了，后端照抄。

**② LLM 供应商 = DeepSeek。**

四份文档里**从头到尾没写过选哪家模型**，这是空白。理由：便宜 + 国内直连不走代理。
代价是结构化输出的 schema 遵守度要靠 §7.2 校验 + §7.4 的 repair 环兜 ——
**这反而让 repair 环有了真实用武之地**，不是纸面设计。

## 四 · 清单里三处刻意的决定

8 个工具，把 §9.2 的每一格铺满：`read_only` 4 / `reversible` 2 / `irreversible` 2 / 无工具（故意缺口）。
**不是为了"工具尽量多"** —— 铺不满格子，审批流就是死代码，图上永远出不了那几种颜色。

| 决定 | 为什么 |
|---|---|
| **`restaurant_booking` 故意不注册** | §1.2 那条 `[blocked · 无可用工具]` 靠的就是"这个工具不存在"。有人顺手补上，那个例子就**悄悄没了**，而且不报错 |
| **`input_schema` 全留 `null`** | 现在写等于**提前把参数定死**，A4 对齐实现时再补。留字段不删，是为了让"**还没定**"和"**忘了填**"看起来不一样 |
| **拿不准的 `side_effect` 一律从严** | 写宽的代价是**该拦的没拦**，写严只是多问一次 |

⚠️ JSON **不支持注释**，而"本该有却没有"是**看不见的** —— 所以 `shared/README.md` 承接这些决定。
另外：`side_effect` 拼错一个字母现在没有任何东西会拦，而它一路决定**要不要拦住一次划钱** ——
读取时未知值应当**走最严或直接启动失败**。

## 五 · A0 骨架：只接通了管道

§12 给 A0 定的完成标准是 `make plan GOAL="..."`。
本次**只接通管道，A0 本身还没完成** —— 标准里「含 assignee 提议 **+ 校验结果**」只兑现了前半句。

| 文件 | 关键点 |
|---|---|
| `Makefile` | §11 的「唯一入口」。**只有 `help` / `plan`** —— `migrate`/`gen-api`/`test`/`eval` 故意不在：**空目标比没有目标更糟**，会让人以为那件事已经做过了（#13 在工具链上的形态）|
| `python_agent/pyproject.toml` | A0 阶段**刻意零依赖**（标准库就够）。`[tool.uv]` 两行不是可选的：`package = false` 免得 uv 去找 build backend；`python-preference = "only-system"` 免得 uv 去 **github** 下载它自己管版的 Python（这台机器上 github 要挂代理）|
| `app/cli.py` | 树的渲染是**真代码**，接上真数据照样用。stdout 显式改 UTF-8 —— Windows 控制台默认编码会把中文打成乱码 |
| `app/_placeholder_plan.py` | ⚠️ **临时文件**，A0 内核写完后删掉。名字带下划线是故意的：它不在 §11 的结构里，就该看起来可疑 |

**占位数据模拟的是 `/v1/plan` 的响应（§3.4），比 §4.2 的完整节点「窄」**：
`status` / `approval` / `assignee_reason` / `locked` / `evidence` 都是**规划之后**的工序补的，
所以这里故意不写 —— 写了就意味着规划器**越权干了 Policy 和执行引擎的活**（ADR-2：提议 ≠ 决定）。

**一处主动改掉的排期**：原计划这一步就把 FastAPI 路由建起来，实际没建 ——
路由要和 CLI 调**同一个核心函数**，而那个函数现在还不存在。
现在建只能把占位数据再包一层，等于提前给"两条入口各写一遍"挖坑，
**正好犯了这个项目最反感的镜像双实现**。所以推到内核写出来时一起接。
（`app/main.py` 与 `app/api/v1/plan.py` 因此暂时不存在 —— 不是遗漏，是排期。）

## 六 · 开发中撞到并修掉的 bug

**容器节点被当成任务**，报了「没提议任何工具」—— 分组本来就不需要工具。

这与**推送 4 修的「容器被当成了任务」是同一个错误**，换了个地方又犯一遍。
容器集合改用与前端 `summarize` **完全相同**的算法（有没有被人当过 `parent_id`）——
**两边口径必须一致**，否则会出现"前端不当任务、后端当任务"的错位。

## 七 · 这台机器上的三处环境事实

| 事实 | 影响 |
|---|---|
| `make` 原本没装 | winget 装 `ezwinports.make 4.4.1`。**坑**：winget 与 SourceForge 都要外网，**代理没开时全失败**（`0x80072efd`）|
| `uv` 原本没装 | `pip install uv` —— pypi **直连可用**，不需要代理 |
| `pip` 装在 `D:\新建文件夹\` | **中文路径**。项目目录是纯 ASCII，所以只要有独立虚拟环境就能绕开 |

## 八 · 验证

- ✅ `make plan GOAL="准备一次日本关西七日游"` 跑通，
  **中文穿过 make → sh → python 未乱码**（这是 Windows 上 make 的经典事故，验掉了）
- ⚠️ **未验证**：从 `cmd` / PowerShell 跑（只从 Git Bash 验过）——
  "没验证"不等于"不能用"，但也不能记成"通过"
- ⚠️ **A0 未完成**：校验那半句还是空的

---

# 推送 9 · 09-15 21:12 · A0 管道接通 + §7.2 ① 生成期校验

```
40db3cb  feat(python_agent): A0 管道接上 DeepSeek + §7.2 ① 生成期校验
```

| | |
|---|---|
| 文件 | 19 个（其中 11 个新增）|
| 规模 | +1922 / −214 |
| 测试 | Python **0 → 37** |

**这一批的主题：A0 的管道全通了 —— 除了"真的调一次模型"。**

## 一 · 落地的位置，一个都没另起名字

全部按 §11 的目录结构放，**没有发明任何一个新模块**：

| 文件 | 干什么 |
|---|---|
| `app/llm/prompts.py` | ⭐ Prompt + 输出 schema（§13 第 8 条：这一块是人的活）|
| `app/llm/client.py` | 调 DeepSeek（json 模式）|
| `app/planner/outline.py` | `level` → 树的**栈组装** + 12-hex ID |
| `app/tools/registry.py` | 读 `shared/tools.json`，**加载时自检** |
| `app/workflows/plan.py` | 编排：generate → check（repair 还没有）|
| `app/domain/validation.py` | §7.2 ① 的 7 条 |
| `app/cli.py` | 接上真管道；**占位数据文件删掉了** |

## 二 · 校验器的两个身份

它不只是"给前端显示问题"——

> §8.1 的**结构断言**（单根 / 层级 / 深度 / 扇出 / 标题长度）记的就是这 7 条。
> **一处实现、两处用**：既是图上给用户看的 `issues`，又是评测的断言。

而且它决定 §7.1 那道闸门的开关（有 `error` 就不许开工）。
所以它一到位，A1 的"结构断言"这一层**就有东西可跑了** ——
不用临时再写一套（那必然漂移）。

## 三 · ⚑ 顺手消掉了两处【本来会出现的重复实现】

校验器要判"谁是谁的爹"，而组装器里已经有一套栈推法。两份**必然漂移**，
而漂移出来的形状恰好是这个项目最怕的：

> **"校验说没问题、组装却抛异常"** —— 而且**两边都不报错**。

所以抽成了两份唯一实现（`level_parents()` / `level_breaks()`），
并加了一条测试钉住"组装器确实用了它" —— 否则哪天有人改回去，不会有任何东西报警。

## 四 · 被已有测试抓住的一个真 bug

重构时把**「多根」误判成了「跳级」**。

根因很具体：`level_parents` 用 `None` **同时**表示"是根"和"跳级" ——
两种情况长得一模一样。而多根是**合法的**（"根应该恰好 1 个"是**校验**的判断，
组装器如实装、不替校验下结论）。

修法就是把这两件事拆成两个函数。**这次是那 17 个已有测试救的场。**

## 五 · 几处刻意的决定

| 决定 | 理由 |
|---|---|
| 组装器**不修复**层级跳跃，直接报错 | 静默把跳级节点挂到别处，用户会看到**一棵错的树却以为是对的**（#13）|
| ID **随机**生成，不按内容 hash | 按内容算的话，改个错别字就换 ID，而别人的 `depends_on` 还写着旧的 → 引用**静默断掉** |
| 缺字段**不补默认值** | 给 `assignee` 补个 `agent`，等于替模型做了一次**用户看不见的**提议 |
| registry 枚举拼错 → **加载就失败** | 不搞"不认识就当最严"：那是人手打的错字，该当场喊出来 |
| 校验跑在**组装之前** | 这 7 条看的是 `level` 表示。所以 `node_id` 一律 `None`，靠 message **指名道姓**（同时给人读的编号和给代码跳的下标）|

Prompt 那边定了四个参数（`level` 基准 = 1 / 字段名照 §3.4 / `assignee` 只填 `agent`·`user` /
`depends_on` 走 schema 用**标题**指认）。**理由记在 `prompts.py` 头部，这里不重复** ——
复制必然漂移，那正是本项目的第一条忌讳。

## 六 · 为什么多了一个 `make prompt`

改 Prompt 不能每次都花钱调模型 —— 所以加了一条**零成本**的回路：
只打印**将要发出去的内容**，不调模型。

它当场抓出了两个**读源码看不出来**的格式错误：

| 撞到的 | 真相 |
|---|---|
| 相邻字符串字面量**自动拼接** | 源码里是分开的两行，**发出去的是一句** |
| 多行 f-string 把**源码缩进带进正文** | 行首凭空多出 4 个空格 |

两条都写进了 `prompts.py` 头部的「格式规矩」（每句一个单行字符串 + 显式 `\n`）。

它还带一条粗检：**目标没出现在 user 消息里就报警** ——
因为这个项目已经三次把参数"收了但没用上"，而那三次**全都不报错**。

## 七 · 又一处缺口（v0.7）

> **§7.2 ① 的 7 条里，没有"模型输出不合 schema"的 code。**

而 DeepSeek 的 json 模式**只保证"是合法 json"**，所以这个情况**一定会撞到**。
当前处理是**报错，而不是发明一个文档里没有的 code**。

另外加了一条测试钉住"产出的 code 不许超出文档里那 7 个" ——
拼错的 code 前端认不出来，而且**没有任何东西会报错**。

## 八 · 验证

- ✅ **37 个测试**（`make test`）
- ✅ `make prompt` / `make plan` 的中文都不乱码
- ✅ 把模型调用 **stub 掉**之后跑通整条链：干净数据出树、坏数据出 `issue` 且**不崩**
  （图装不出来时明确说一句，不留白）
- ⚠️ **未验证：从没真调过 DeepSeek** —— 还没有 key。
  `base_url` / 模型名 / `response_format` 都是我按 OpenAI 兼容接口的常规值填的**默认值**，
  第一次真调就是验它们的时候

---

# 附录 · 文档版本演进

| 版本 | 变更 |
|---|---|
| **v0.2** | 初始版本（推送 1）|
| **v0.3** | 前端 demo 设计评审的 9 处裁决（推送 2）|
| **v0.4** | §4.2 补 `result_summary` + `assignee_reason` 枚举（推送 4）|
| **v0.5** | §7.3 把 `E_MISSING_EVIDENCE` 限定为 `assignee=agent`（推送 4）|
| **v0.6** | §7.2 第 ③ 组补"在哪跑"与危害说明（**整组此前从未实现**）；§14.2 新增反模式 #13「静默失败」；§5.3 新增「删除节点」+ 修正附录 A 一处**悬空引用**（推送 6）|

> **这些修订都是"写代码时撞上"才做的** —— 写代码比读文档更容易发现矛盾。
>
> **缺口一共 6 处**：`result_summary` 无处可存、分配原因无处可放、
> 证据规则没写作用域、失败处置没有定义（v0.3–v0.5）；
> **依赖图校验整组没实现**、**删除节点的规则代码里有、文档里没有**（v0.6）。
> 它们都不是读文档时能看出来的，而是写到那一步才撞上的。

> ⚑ 另外 v0.6 还纠正了**一个不属于缺口的东西**：附录 A 里一条**悬空引用**
> （`E2` 指向不存在的 §4.2 复制分支，而"复制"这个需求本身也不存在）。
> 这和 #13 一样，是**实测才发现的** —— 读文档看不出来，
> 因为附录是清单，读的人不会逐条回查。

---

# 附录 · 当前状态

| 层 | 对应阶段 | 状态 |
|---|---|---|
| 文档 | —— | ✅ v0.6 + ARCHITECTURE v1.0 + INTEGRATION v1.0（**另有 8 处待回写**：推送 8 第二节那 7 条 + 推送 9 第七节那条）|
| **前端 demo** | 属于 A7 | ✅ **可运行**（数据全 mock）|
| **工具注册表** | §4.4 | ✅ `shared/tools.json`（8 个工具）|
| **Python 引擎** | A0–A5 | 🟡 **管道全通，只差一个 key** —— 但**从没真调过模型**（A0 尚未验收）|
| Java Host | A6 | ❌ 未开始 |

**前端**：279 个测试通过 · 构建通过 · lint 无告警
**Python**：**37 个**测试通过

快速开始：

```bash
# 前端 demo
cd frontend
npm install
npm run dev      # → http://localhost:5173/
npm test         # 279 个测试

# Python 引擎（A0）
make test                              # 37 个测试（不花钱、不用 key）
make prompt GOAL="准备一次日本关西七日游"   # 只打印将要发出去的内容（零成本）
make plan   GOAL="准备一次日本关西七日游"   # ⚠️ 要先在 python_agent/.env 里填 DEEPSEEK_API_KEY
```

> ⚠️ `make` 要**新开的终端**才认得（winget 装完改的是 PATH，旧会话看不到）。
> 另外它只从 Git Bash 验过，没从 `cmd` / PowerShell 验过。

> ⚠️ **类型检查用 `npm run build`（或 `tsc -b`），不要用 `tsc --noEmit`** ——
> 根 `tsconfig.json` 是 `files: []` + project references，
> `--noEmit` 在它上面跑会**检查 0 个文件、永远通过**。

### demo 里还没做的（FR-6 剩下的）

| 需求 | 状态 |
|---|---|
| 拖拽 / 增删 / 调层级 | ✅ 做了 |
| **改名** | ❌ 没做（FR-6 的最后一项）|

另有两处**只有本地、没有并发正确性**：
结构性编辑没有 `revision` 乐观锁、没有 409、没有按节点串行保存队列 ——
那些都在 A6（§5.3 铁律 #2 / #4）。
**单机演示下感觉不到差别，但真做起来，这里是并发正确性的全部所在。**
