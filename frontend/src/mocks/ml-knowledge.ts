/**
 * mock 数据集 · ①  知识型
 *
 * 来源：DEV_DOC §8.3 评测集种子 #1「整理一下机器学习的知识体系」
 *       断言：深度=3、单根、含"监督学习"
 *
 * 这份数据要压测布局算法的哪个维度：
 *   1 个根、4 个一级分支、13 个叶子，深度 3 —— 扇出偏宽、层数适中，
 *   是最典型的"知识图谱"形态。
 *
 * 状态刻意做成【全绿】：知识型导图的产物都是 agent 已经"想清楚"的内容，
 * 不像任务型要等人确认。这正好和 japan-trip 那份形成视觉对比 ——
 * 切换数据集时能一眼看出"全绿"和"五颜六色"的区别。
 */
import type { OutlineNode } from '../types/outline'
import { node } from './_helper'

export const mlKnowledge: OutlineNode[] = [
  node('a10000000000', null, 0, '整理一下机器学习的知识体系'),

  /* ── 一级分支 ─────────────────────────────────────────── */
  node('a10000000001', 'a10000000000', 0, '监督学习'),
  node('a10000000002', 'a10000000000', 1, '无监督学习'),
  node('a10000000003', 'a10000000000', 2, '强化学习'),
  node('a10000000004', 'a10000000000', 3, '评估与实践'),

  /* ── 监督学习 ─────────────────────────────────────────── */
  node('a10000000011', 'a10000000001', 0, '线性回归'),
  node('a10000000012', 'a10000000001', 1, '决策树'),
  // 只给这一个节点挂证据，用来验证「点节点 → 看证据链」的面板是真的能出东西，
  // 而不是每个节点都挂（那会让人分不清面板到底绑定对了没有）
  node('a10000000013', 'a10000000001', 2, '支持向量机', {
    evidence: {
      tool: 'knowledge_lookup',
      args: { topic: 'SVM', detail: 'kernel_methods' },
      result_ref: 'tool_call_a1f3',
      elapsed_ms: 1240,
      verified_at: '2026-09-12T10:15:02Z',
    },
  }),
  node('a10000000014', 'a10000000001', 3, '神经网络'),

  /* ── 无监督学习 ───────────────────────────────────────── */
  node('a10000000021', 'a10000000002', 0, '聚类'),
  node('a10000000022', 'a10000000002', 1, '降维'),
  node('a10000000023', 'a10000000002', 2, '异常检测'),

  /* ── 强化学习 ─────────────────────────────────────────── */
  node('a10000000031', 'a10000000003', 0, '价值函数方法'),
  node('a10000000032', 'a10000000003', 1, '策略梯度'),
  node('a10000000033', 'a10000000003', 2, '模型基方法'),

  /* ── 评估与实践 ───────────────────────────────────────── */
  node('a10000000041', 'a10000000004', 0, '交叉验证'),
  node('a10000000042', 'a10000000004', 1, '偏差方差权衡'),
  node('a10000000043', 'a10000000004', 2, '过拟合与正则化'),
]
