/**
 * mock 数据集 · ④  坏数据演示
 *
 * ⚠️ 这份【不是】§8.3 的评测集种子 —— 它是我为 D2 专门造的展示样本。
 *    前三份是"模型可能产出的合理输出"，这一份是"不该存在但会存在的输出"。
 *
 * 为什么需要它：读数据的那一半代码都在处理坏数据，但这些路径在好的数据集上
 * 永远不会触发。不造一份坏数据，就没法验证它们真的工作 ——
 * 也没法在界面上看见"坏数据被显式显示而不是静默丢弃"这个原则的落地效果。
 *
 * 这份数据同时踩了 6 个坑，**分属两张不同的图**：
 *
 *   ── §7.2 第 ② 组：层级树（parent_id / order）──────────────
 *   ① d4…0002 出现两次          → E_DUPLICATE_ID（后出现的被忽略）
 *   ② d4…0003 的父不存在        → E_ORPHAN_PARENT
 *   ③ d4…0004 ⇄ d4…0005 互为父  → E_CYCLE_PARENT（两个节点各报一条）
 *   ④ 根的孩子们 order 是 [0,2,…]→ W_ORDER_INVALID（缺 1）
 *
 *   ── §7.2 第 ③ 组：依赖图（depends_on）─────────────────────
 *   ⑤ d4…0006 依赖一个不存在的 id → E_DANGLING_DEP
 *   ⑥ d4…0007 ⇄ d4…0008 互相依赖  → E_CYCLE_DEP（两个节点各报一条）
 *
 * ⚑ ⑤ 和 ⑥ 单独用新节点、不叠在 ② ③ 身上，是有意的：
 *   一个节点同时有两三种毛病的话，界面上根本看不出哪条对应哪个问题，
 *   这份样本就失去了"教学"的作用。
 *
 * 预期结果：roots = 1 棵（根 + 0001 + 0002 + 0006 + 0007 + 0008）
 *          detached = [0003, 0004, 0005]
 *          issues = 1 重复id + 1 孤儿 + 2 环 + 1 order
 *                 + 1 依赖悬空 + 2 依赖环      （全部为 error，除 order 外）
 */
import type { OutlineNode } from '../types/outline'
import { node } from './_helper'

export const corruptSample: OutlineNode[] = [
  node('d40000000000', null, 0, '演示：坏数据', { status: 'running' }),

  /* ① 正常的两个子节点 —— 但 order 是 [0, 2]，缺了 1 → W_ORDER_INVALID */
  node('d40000000001', 'd40000000000', 0, '正常子节点 A'),
  node('d40000000002', 'd40000000000', 2, '正常子节点 B'),

  /* ② 孤儿：父 id 指向一个不存在的节点。
   *    `d4deadbeef00` 是 12 位 hex，格式合法但没人用它当 id ——
   *    现实里这对应「拖拽操作做了一半」或「AI 的操作列表没落全」。 */
  node('d40000000003', 'd4deadbeef00', 0, '孤儿节点'),

  /* ③ 环：X 的父是 Y，Y 的父是 X。
   *    沿 parent_id 向上走会无限绕圈 —— 这就是为什么 classify() 必须有 onPath 集合。
   *    现实里这对应「人把 A 拖进 B、又把 B 拖进 A」（§5.3 允许拖拽调层级）。 */
  node('d40000000004', 'd40000000005', 0, '环上的节点 X'),
  node('d40000000005', 'd40000000004', 0, '环上的节点 Y'),

  /* ① 重复 id：与上面那个 d4…0002 撞了。
   *    Map 会静默覆盖，所以这条如果没被检测出来，界面上会莫名少一个节点。 */
  node('d40000000002', 'd40000000000', 1, '重复 id 的节点'),

  /* ⑤ 依赖悬空：depends_on 指着一个不存在的 id。
   *    ⚠️ 这是六个坑里**最难发现**的一个 ——
   *    环至少还能从"任务不动了"看出来，悬空依赖只是让【这一个】任务
   *    永远静静等着，而界面上它就是一个普普通通的「待办」。
   *    现实里这对应「AI 的操作列表里删掉了一个节点，却忘了清理引用它的依赖」。 */
  node('d40000000006', 'd40000000000', 3, '依赖悬空的节点', {
    depends_on: ['d4deadbeef99'],
  }),

  /* ⑥ 依赖环：两个任务互相等对方先做完，谁都不会先跑。
   *    注意这与 ③ 是**两张不同的图**上的环 ——
   *    ③ 是层级树成环，⑥ 是依赖图成环，检测算法完全不同（见 lib/outline.ts）。
   *    现实里这对应「AI 改稿时把依赖关系改反了」。 */
  node('d40000000007', 'd40000000000', 4, '依赖环上的节点 P', {
    depends_on: ['d40000000008'],
  }),
  node('d40000000008', 'd40000000000', 5, '依赖环上的节点 Q', {
    depends_on: ['d40000000007'],
  }),
]
